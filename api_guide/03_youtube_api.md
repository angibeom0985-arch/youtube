# 📺 YouTube Data API 키 발급하기

## 📌 개요

- 발급처: Google Cloud Console
- 비용: 무료 (일일 할당량 10,000 units)
- 소요 시간: 약 10분
- 신용카드: 불필요 (무료 할당량만 사용 시)
- 용도: YouTube 영상 검색, 정보 조회, 모멘텀 분석

## 🎯 YouTube Data API의 역할

이 웹사이트에서 YouTube Data API는 다음 기능에 사용됩니다:

- ✅ YouTube 영상 검색
- ✅ 영상 메타데이터 조회 (조회수, 좋아요, 댓글 수)
- ✅ 채널 구독자 수 확인
- ✅ 모멘텀 분석 (조회수 증가 추세)

## 📝 발급 절차

### 1단계: Google Cloud Console 접속

1. 브라우저에서 다음 주소로 이동:
   ```
   https://console.cloud.google.com/
   ```

2. Google 계정으로 로그인

3. 첫 방문 시:
   - 서비스 약관 동의
   - 국가 선택 (대한민국)

### 2단계: 프로젝트 생성

1. 상단 네비게이션 바에서 프로젝트 선택 드롭다운 클릭

2. "새 프로젝트" 버튼 클릭

3. 프로젝트 정보 입력:
   - 프로젝트 이름: 예) `youtube-script-generator`
   - 위치: 조직 없음 (개인 사용)

4. "만들기" 버튼 클릭

5. 프로젝트 생성 완료까지 약 30초 대기

6. 상단 드롭다운에서 방금 만든 프로젝트 선택

### 3단계: YouTube Data API v3 활성화

1. 왼쪽 메뉴에서 "API 및 서비스" > "라이브러리" 클릭

2. 검색창에 `YouTube Data API v3` 입력

3. "YouTube Data API v3" 클릭 (YouTube 로고가 있는 것)

4. "사용 설정" 버튼 클릭

5. API 활성화 완료 (약 10초)

### 4단계: API 키 생성

1. 왼쪽 메뉴에서 "API 및 서비스" > "사용자 인증 정보" 클릭

2. 상단의 "+ 사용자 인증 정보 만들기" 버튼 클릭

3. "API 키" 선택

4. API 키가 생성되고 팝업 창에 표시됩니다:
   ```
   AIzaSy...xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

5. "복사" 버튼을 클릭하여 API 키 복사

6. ⚠️ 중요: 안전한 곳에 저장
   - 메모장에 임시 저장
   - 나중에 `.env` 파일에 설정

### 5단계: API 키 제한 설정 (권장)

보안을 위해 API 키에 제한을 설정하는 것이 좋습니다:

1. 생성된 키 이름 옆의 연필 아이콘 클릭

2. "API 제한사항" 섹션:
   - "키 제한" 선택
   - "YouTube Data API v3" 체크
   - (다른 API는 체크 해제)

3. "애플리케이션 제한사항" (선택):
   
   옵션 A: HTTP 리퍼러 제한 (웹사이트용)
   - "HTTP 리퍼러" 선택
   - 웹사이트 URL 추가:
     ```
     https://yourdomain.com/*
     http://localhost:*
     ```
   
   옵션 B: IP 주소 제한 (서버용)
   - "IP 주소" 선택
   - 서버 IP 주소 입력

   옵션 C: 제한 없음 (개발 중)
   - 테스트 단계에서는 제한 없이 사용 가능
   - 배포 전에 반드시 제한 설정

4. "저장" 버튼 클릭

## 💰 비용 및 할당량

### 무료 할당량

- 일일 할당량: 10,000 units
- 비용: 완전 무료

### 할당량 계산

| 작업 | 비용 (units) |
|------|--------------|
| 영상 검색 (search.list) | 100 units |
| 영상 정보 (videos.list) | 1 unit |
| 채널 정보 (channels.list) | 1 unit |

### 예상 사용량

모멘텀 헌터 1회 실행:
- 검색 요청: 100 units
- 영상 정보 100개: 100 units
- 채널 정보: ~2 units
- 총 약 200~300 units

일일 사용 가능 횟수:
- 모멘텀 헌터: 약 30~50회
- 일반 사용자 기준: 충분히 여유로움

### 할당량 초과 시

무료 할당량 10,000 units 초과 시:

옵션 1: 다음 날까지 대기
- 매일 자정(UTC)에 할당량 초기화
- 비용 없음

옵션 2: 결제 계정 연결하여 할당량 구매
- Google Cloud Console에서 결제 계정 연결
- 초과분: 약 $0.50~$1.00 / 10,000 units
- 자동 과금

## 🔍 할당량 모니터링

### 할당량 확인 방법

1. Google Cloud Console 접속

2. "API 및 서비스" > "대시보드" 클릭

3. "YouTube Data API v3" 클릭

4. "할당량" 탭 선택

5. 현재 사용량 확인:
   - Queries per day
   - 사용률 그래프

### 알림 설정 (선택)

할당량 80% 도달 시 이메일 알림:

1. "API 및 서비스" > "할당량" 클릭

2. YouTube Data API v3 찾기

3. "할당량 알림" 설정

4. 이메일 주소 입력 및 저장

## ✅ 확인 방법

API 키가 제대로 작동하는지 확인:

### 방법 1: 브라우저에서 직접 테스트

다음 URL을 브라우저 주소창에 붙여넣기 (YOUR_API_KEY를 실제 키로 교체):

```
https://www.googleapis.com/youtube/v3/search?part=snippet&q=프로그래밍&type=video&maxResults=5&key=YOUR_API_KEY
```

성공 시: JSON 형식의 검색 결과가 표시됩니다
```json
{
  "kind": "youtube#searchListResponse",
  "items": [...]
}
```

실패 시: 에러 메시지가 표시됩니다
```json
{
  "error": {
    "code": 400,
    "message": "Bad Request"
  }
}
```

### 방법 2: curl 명령어 (터미널)

```bash
curl "https://www.googleapis.com/youtube/v3/search?part=snippet&q=프로그래밍&type=video&maxResults=1&key=YOUR_API_KEY"
```

### 방법 3: 웹사이트에서 테스트

1. `.env` 파일에 API 키 설정 (다음 가이드 참고)
2. 웹사이트 실행
3. 모멘텀 헌터 기능 테스트

## 🔧 문제 해결

### "API key not valid" 오류

원인:
- API 키가 잘못 복사됨
- API 키 제한사항 설정 문제

해결 방법:
1. API 키를 다시 복사하여 공백 없이 붙여넣기
2. Google Cloud Console에서 키 제한사항 확인
3. YouTube Data API v3가 제한에 포함되어 있는지 확인

### "Access Not Configured" 오류

원인:
- YouTube Data API v3가 활성화되지 않음

해결 방법:
1. Google Cloud Console > "API 및 서비스" > "라이브러리"
2. "YouTube Data API v3" 검색
3. "사용 설정" 버튼 클릭

### "Quota exceeded" 오류

원인:
- 일일 할당량 10,000 units 초과

해결 방법:
1. 무료 사용: 다음 날(UTC 자정) 대기
2. 유료 사용: 결제 계정 연결하여 할당량 증가

### "Daily Limit Exceeded" 오류

원인:
- 프로젝트의 일일 할당량 도달

해결 방법:
1. Google Cloud Console > "API 및 서비스" > "할당량"
2. 할당량 증가 요청 (무료)
3. 또는 새 프로젝트 생성

## 🎓 추가 정보

### YouTube Data API 할당량 상세

더 복잡한 기능 사용 시 참고:

| API 메서드 | 할당량 비용 |
|------------|-------------|
| search.list | 100 |
| videos.list | 1 |
| channels.list | 1 |
| commentThreads.list | 1 |
| playlistItems.list | 1 |
| subscriptions.list | 1 |

### 할당량 최적화 팁

1. 캐싱 활용:
   - 자주 조회하는 영상 정보는 캐싱
   - Redis 또는 메모리 캐시 사용

2. 배치 요청:
   - videos.list는 한 번에 최대 50개 영상 조회 가능
   - `id=video1,video2,video3` 형식 사용

3. 필요한 필드만 요청:
   - `part=snippet` 대신 `part=snippet&fields=items(id,snippet(title))`
   - 할당량은 동일하지만 응답 속도 향상

## 📌 다음 단계

✅ YouTube Data API 키를 발급받았습니다!

선택사항:
- TTS/STT 기능이 필요하다면: [04_google_cloud_api.md](04_google_cloud_api.md)
- 기타 고급 기능: [05_optional_apis.md](05_optional_apis.md)

바로 환경 설정으로 이동: [06_environment_setup.md](06_environment_setup.md)

---

💡 팁: YouTube Data API는 무료 할당량만으로도 대부분의 개인 프로젝트에 충분합니다!
